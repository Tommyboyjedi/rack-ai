# Keep native CPU support pools bounded inside the managed GPU container.
# The base is supplied as a pinned local image; no packages or models are installed.
ARG BASE_IMAGE
FROM ${BASE_IMAGE}
ENV OMP_NUM_THREADS=4 OPENBLAS_NUM_THREADS=4 MKL_NUM_THREADS=4 NUMEXPR_NUM_THREADS=4
